
mkdir -p ./pages.json

count=$(ls ./id | wc -l)

for _F in ./id/*
do
    echo "$((count--)) ${_F}"
    _F=$(basename ${_F})
    curl -s https://radio.garden/api/ara/content/page/${_F} | jq . > ./pages.json/${_F}.json
done
